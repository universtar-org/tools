name: Release Tools

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.7"

      - name: Build Binaries
        run: |
          mkdir dist

          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o dist/ust .

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
